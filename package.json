{
  "name": "formato-desktop",
  "version": "2.6.0",
  "private": true,
  "description": "Formato - Native Python & PySide6 Desktop Image Converter",
  "scripts": {
    "build": "python3 -m unittest discover -s tests -p 'test_*.py'",
    "test": "python3 -m unittest discover -s tests -p 'test_*.py'"
  }
}
