name: Build Multi-Platform Executables & Installers

permissions:
  contents: write

on:
  push:
    branches: [ "main", "master" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  # =======================================================
  # 1. WINDOWS BUILD (PyInstaller + Inno Setup)
  # =======================================================
  build-windows:
    name: Build Windows (.exe Setup)
    runs-on: windows-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Executable with PyInstaller
        run: |
          pyinstaller --noconfirm --onedir --windowed `
            --icon="assets/icon.ico" `
            --add-data "assets;assets" `
            --name "Formato" `
            main.py

      - name: Generate Inno Setup Script
        run: |
          @'
          [Setup]
          AppName=Formato - Professional Image Studio
          AppVersion=1.0.0
          AppPublisher=Ali Kamrani
          DefaultDirName={autopf}\Formato
          DefaultGroupName=Formato
          UninstallDisplayIcon={app}\Formato.exe
          Compression=lzma2/ultra
          SolidCompression=yes
          OutputDir=installer_output
          OutputBaseFilename=Formato-Windows-Setup
          SetupIconFile=assets\icon.ico

          [Tasks]
          Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked

          [Files]
          Source: "dist\Formato\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs

          [Icons]
          Name: "{autoprograms}\Formato"; Filename: "{app}\Formato.exe"
          Name: "{autodesktop}\Formato"; Filename: "{app}\Formato.exe"; Tasks: desktopicon

          [Run]
          Filename: "{app}\Formato.exe"; Description: "{cm:LaunchProgram,Formato}"; Flags: nowait postinstall skipifsilent
          '@ | Out-File -FilePath installer.iss -Encoding utf8

      - name: Compile Setup Installer with Inno Setup
        run: |
          iscc installer.iss

      - name: Upload Windows Setup Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Formato-Windows-Setup
          path: installer_output/Formato-Windows-Setup.exe


  # =======================================================
  # 2. MACOS BUILD (PyInstaller + DMG)
  # =======================================================
  build-macos:
    name: Build macOS (.dmg)
    runs-on: macos-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build macOS App Bundle
        run: |
          pyinstaller --noconfirm --windowed \
            --icon="assets/icon.ico" \
            --add-data "assets:assets" \
            --name "Formato" \
            main.py

      - name: Create DMG File
        run: |
          hdiutil create -volname "Formato" -srcfolder "dist/Formato.app" -ov -format UDZO "dist/Formato-macOS.dmg"

      - name: Upload macOS DMG Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Formato-macOS
          path: dist/Formato-macOS.dmg


  # =======================================================
  # 3. LINUX BUILD (PyInstaller + Tar.gz Archive)
  # =======================================================
  build-linux:
    name: Build Linux (.tar.gz)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Install System Dependencies for PySide6/Qt
        run: |
          sudo apt-get update
          sudo apt-get install -y libxcb-cursor0 libxcb-xinerama0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-render-util0 libxcb-shape0 libegl1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Linux Binary
        run: |
          pyinstaller --noconfirm --onedir --windowed \
            --icon="assets/icon.ico" \
            --add-data "assets:assets" \
            --name "Formato" \
            main.py

      - name: Compress Linux Output
        run: |
          cd dist
          tar -czvf Formato-Linux-x64.tar.gz Formato

      - name: Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Formato-Linux
          path: dist/Formato-Linux-x64.tar.gz


  # =======================================================
  # 4. AUTOMATIC GITHUB RELEASE (On Tag Push, e.g., v1.0.0)
  # =======================================================
  release:
    name: Publish Github Release
    needs: [build-windows, build-macos, build-linux]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest

    steps:
      - name: Download Windows Installer
        uses: actions/download-artifact@v4
        with:
          name: Formato-Windows-Setup
          path: ./release-files

      - name: Download macOS DMG
        uses: actions/download-artifact@v4
        with:
          name: Formato-macOS
          path: ./release-files

      - name: Download Linux Archive
        uses: actions/download-artifact@v4
        with:
          name: Formato-Linux
          path: ./release-files

      - name: Create Release and Upload Files
        uses: softprops/action-gh-release@v2
        with:
          files: ./release-files/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
